name: mystic_geo_store
description: "Geo dataset (countries, states, cities, dial codes, currencies) with search and lookup support."
version: 1.1.2
homepage: https://github.com/s1lent-warrior/mystic_geo_store

environment:
  sdk: ^3.4.0

dependencies:
  meta: ^1.12.0

dev_dependencies:
  archive: ^3.6.1
  lints: ^6.0.0
  test: ^1.29.0
